<!DOCTYPE html>
<html>
  <head>
    <title>Images</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Images</h1>
    <ul>
      <% for (let image of images) { %>
        <li><img alt="<%=image.name%>" src="<%=image.url%>"></li>
      <% }   %>
    </ul>
  </body>
</html>
